{
  "name": "peacecoparent",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "install:all": "npm install --prefix backend && npm install --prefix frontend-next",
    "dev:backend": "npm run dev --prefix backend",
    "dev:frontend": "npm run dev --prefix frontend-next",
    "dev:frontend:legacy": "npm run dev --prefix archive/old-frontend",
    "db:migrate": "npm run migrate --prefix backend"
  }
}
